/***
package com.example.vrexpo.Admin;
import com.example.vrexpo.CreateAccount;

/***
 * This class allows admin to do the following
 * Manage user accounts (creating, editing, deleting accounts)
 * Handle user permissions and roles
 ***/

/***
public class UserManagement {

    /** Create Account Method to create accounts and add user to database **/
   /** public void createUserAccount(CreateAccount createAccount) {
        createAccount.registerUser();

    }

    /** Edit User Account Method to edit existing user accounts
     * Admin updates user information such as username, password, permissions, etc.
     */

   /** public void editUserAccount(CreateAccount editAccount, String phone, String newName, String newDob, String newGender,
                                String newEmail, String newAddress, String newPassword) {
        editAccount.editUserDetails(phone, newName, newDob, newGender, newEmail, newAddress, newPassword);
    }

    /** Delete user accounts from database to ensure data integrity and security **/
   /** public void deleteUserAccount (CreateAccount deleteAccount, String phone) {
        deleteAccount.deleteUserAccount(phone);

    }

    /** Handle user permissions and roles **/
    /**public void handleUserPermissions (CreateAccount handleUserRoles, String phone, boolean isAdmin, boolean isPatient, boolean isTherapist) {
        handleUserRoles.manageUserPermissions(phone, isAdmin, isPatient, isTherapist);

    }

} **/
